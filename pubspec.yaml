name: searchify
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/Jamshidmirzo/to_upper_case_first_character
homepage: https://jamshids-portfolio.netlify.app

environment:
  sdk: ^3.4.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
